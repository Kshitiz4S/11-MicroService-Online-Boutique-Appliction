First run the script.sh

#Make it executable

sudo chmod +x script.sh
./script.sh                                                                                                             
This will install docker, docker-compose, openssl and download the certificate in the current folder.

Then run docker-compose up -d
